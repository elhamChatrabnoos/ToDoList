import 'package:flutter/widgets.dart';

class CustomSizes{
  static const bigHeight = SizedBox(height: 40);
  static const normalHeight = SizedBox(height: 30);
  static const littleHeight = SizedBox(height: 20);

  static const littleWidth = SizedBox(width: 10);
  static const normalWidth = SizedBox(width: 20);

  static double? bigText = 30;
  static const normalText = 20;
  static const littleText = 10;

}